export const isLoggedIn = state => state.auth.isLoggedIn;
export const isRefreshing = state => state.tasks.tasks;
export const user = state => state.tasks.tasks;
export const token = state => state.tasks.tasks;
